Do not use excessive emojis.

Do not use emdashes.

Do not pull in arbitrary, unnecessary dependencies for simple things - keeping dependency lists short and manageable are a core design goal.

You can pull in libraries / packages where it makes sense to do so, but they need to be thought about carefully.
